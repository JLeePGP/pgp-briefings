# Block only the raw SEC CSV databases inside the data folder
data/*.csv
data/*.CSV

# Block the hidden local Netlify cache folder inside data
data/.netlify/

# Block global pipeline data and keys in the root or data folders
prospects_top100.csv
nadir_market_vault.json
pgp_config.json
claude_briefing_*.txt
.env

# Block local website build directories
netlify_build/
nadir_vault/